[tool.poetry]
name = "pysnake"
version = "0.1.0"
description = ""
authors = ["nour <nour.soubat@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
pygame = "^2.5.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
